import math

#Math libraries

# distance = math.dist([x1, y1], [x2, y2])

# circumference = 2 * math.pi * radius

# l, w, h = args
#   volume = l * w * h
        
# volume = math.pi * (r ** 2) * h        

#largest = max(a, b, c)

name="hellow world"
print(name.strip("h"))